namespace JReact.Playfab_Interact
{
    //the enum to store the login type
    public enum ConnectionType { NotSet = 0, Silent, Facebook }

    //the console message types
    public enum ConsoleMessageTypes { Playfab_Interact, Playfab_CloudScript }

}
